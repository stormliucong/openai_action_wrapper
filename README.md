### OPENAI Action Wrapper

- Some API call should be modified in when try to use OPENAI to get access
- This wrapper provider a custermized API which will call the original API first but then modifying the response to make it suitable for OPENAI Action
